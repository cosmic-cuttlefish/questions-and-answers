events { }
http {
  upstream appservers {
        least_conn;
        server localhost:3000;
        server localhost:3000;
        server localhost:3000;
  }
  server {
        location / {
            proxy_pass http://appservers;
         }
  }
}