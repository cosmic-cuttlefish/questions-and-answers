events { }
http {
  upstream appservers {
        least_conn;
        server localhost:3000;
        server localhost:3000;
        server localhost:3000;
  }
  server {
        listen 8080;
        server_name appservers;

        location / {
            proxy_pass http://appservers;
         }
  }
}